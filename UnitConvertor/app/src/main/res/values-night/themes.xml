<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.UnitConvertor" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/dark_background</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="appBackground">
        <item name="android:background">@color/dark_background</item>
    </style>
    <style name="convertBtn">
        <item name="neumorph_shadowColorLight">@color/dark_light_color</item>
        <item name="neumorph_shadowColorDark">@color/dark_shadow_color</item>
    </style>
</resources>